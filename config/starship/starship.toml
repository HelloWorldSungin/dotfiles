# Starship configuration
# Timeout for commands executed by Starship (in milliseconds)
# Set to 60000ms (60 seconds) for large Git repositories
command_timeout = 60000

[git_status]
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"
